CREATE TABLE `users` (
                         `id` int NOT NULL,
                         `firstname` varchar(250) DEFAULT NULL,
                         `lastname` varchar(250) DEFAULT NULL,
                         PRIMARY KEY (`id`)
);